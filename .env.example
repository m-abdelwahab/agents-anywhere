# ============================================================
# Agents Anywhere — Environment Variables
# ============================================================
# Copy to .env and fill in values, or set directly in Railway when deploying the template.

# --- SSH Authentication (set at least one) -------------------

# Your SSH public key — recommended for computers.
# Get it with: cat ~/.ssh/id_ed25519.pub
# Format: ssh-ed25519 AAAA... user@host
SSH_PUBLIC_KEY=

# For teams, each member can set their own key variable:
# SSH_PUBLIC_KEY_ALICE=ssh-ed25519 AAAA... alice@laptop
# SSH_PUBLIC_KEY_BOB=ssh-ed25519 AAAA... bob@desktop

# Password for SSH access — simpler, great for mobile.
# Use a strong password (16+ chars) — the SSH port is exposed to the internet.
SSH_PASSWORD=
